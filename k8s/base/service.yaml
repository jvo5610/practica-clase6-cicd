apiVersion: v1
kind: Service
metadata:
  name: formatec-api
  labels:
    app: formatec-api
spec:
  type: ClusterIP
  selector:
    app: formatec-api
  ports:
    - name: http
      port: 80
      targetPort: http

